<%= form_with(model: [post, post.comments.build]) do |form| %>
    
    <div class="">
        <%= form.rich_text_area :body, class: "form-control mb-2 mt-3" %>
        <%= form.submit "Reply", class: "btn btn-primary mt-1 " %>
    </div>

<% end %>